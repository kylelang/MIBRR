useDynLib(MIBRR)

import(RcppEigen)
import(methods)

importFrom(Rcpp, evalCpp)
importFrom(mice, mice, quickpred, complete)
importFrom(lavaan, lavaan, inspect)
importFrom(optimx, optimx)
importFrom(mvtnorm, rmvnorm)
importFrom(stats,
           anova,
           lm,
           pgamma,
           rexp,
           rgamma,
           rnorm,
           sd,
           var,
           density)
importFrom(rlecuyer,
           .lec.SetPackageSeed,
           .lec.CreateStream,
           .lec.ResetNextSubstream,
           .lec.GetState,
           .lec.DeleteStream)

export(mibl,
       miben,
       mibrrW,
       mibrrL,
       ben,
       bl,
       postPredict,
       getImpData,
       getField,
       getParams,
       vanilla)

### NOTE: The following non-exported, undocumented functions are available for
###       testing/debugging. See the relevant source code for details of their
###       implementation.

## drawInvGamma
## drawMvn
## calcIncGamma
## drawInvGauss
## printObsIndices
## printMissIndices
